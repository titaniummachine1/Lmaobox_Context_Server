## Function/Symbol: playerlist.SetPriority

> Set aimbot priority for a player

### Curated Usage Examples

```lua
local player = entities.GetByIndex(idx)
if player then
    playerlist.SetPriority(player, 10) -- high priority
end
```

### Notes
- Use to mark targets (medics, spies) for higher priority

