## Function/Symbol: Entity.GetClass

> Get the entity's classname

### Curated Usage Examples

```lua
local ent = entities.GetByIndex(idx)
if ent then
    print(ent:GetClass())
end
```

### Notes

- Use to filter buildings/projectiles/etc

