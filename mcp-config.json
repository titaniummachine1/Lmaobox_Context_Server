{
  "_comment": "TEMPLATE - Use scripts/setup-mcp-config.ps1 to generate the correct config for your system",
  "mcpServers": {
    "lmaobox-context": {
      "type": "stdio",
      "command": "python",
      "args": ["REPLACE_WITH_REPO_ROOT/launch_mcp.py"],
      "cwd": "REPLACE_WITH_REPO_ROOT"
    }
  }
}
