## Function/Symbol: warp.TriggerWarp

> Trigger a warp (small speedhack/dash)

### Curated Usage Examples

```lua
if warp.CanWarp() then
    warp.TriggerWarp()
end
```

### Notes
- Use warp.GetChargedTicks to check charge level first
