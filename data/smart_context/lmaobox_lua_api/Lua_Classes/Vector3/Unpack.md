## Function/Symbol: Vector3.Unpack

> Unpack Vector3 into x, y, z components

### Curated Usage Examples

```lua
local pos = entity:GetAbsOrigin()
local x, y, z = pos:Unpack()
print("X: " .. x .. " Y: " .. y .. " Z: " .. z)
```

### Notes

- Returns three separate numbers

